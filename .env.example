# URL of your self-hosted PocketBase instance (see DEPLOY.md).
# Leave empty in production — PocketBase serves the built app itself, so API
# calls are same-origin. Set this for local development to point at your
# Synology NAS, e.g.:
VITE_POCKETBASE_URL=http://192.168.1.50:8090
